/// Agora client role for RTC channel.
/// Use [publisher] for calling or live host (can publish audio).
/// Use [subscriber] for audience (listen only).
enum AgoraClientRole {
  publisher,
  subscriber,
}
